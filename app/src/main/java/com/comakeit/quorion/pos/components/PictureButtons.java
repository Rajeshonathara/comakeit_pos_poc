package com.comakeit.quorion.pos.components;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class PictureButtons extends LinearLayout {
    public PictureButtons(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
